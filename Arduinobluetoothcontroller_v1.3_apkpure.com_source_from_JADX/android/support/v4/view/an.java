package android.support.v4.view;

import android.view.ViewGroup.MarginLayoutParams;

interface an {
    int mo296a(MarginLayoutParams marginLayoutParams);

    int mo297b(MarginLayoutParams marginLayoutParams);
}
